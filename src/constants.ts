// Configuration keys
export const KEY_DEFAULT_WIDTH = 'toggleSidebarWidth.defaultWidth';

// Storage keys
export const KEY_STORED_WIDTH = 'toggle-sidebar-width.storedWidth';

// Default values
export const DEFAULT_DEFAULT_WIDTH = 250;
export const DEFAULT_STORED_WIDTH = 600;
